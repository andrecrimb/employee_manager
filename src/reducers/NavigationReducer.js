// import { RootRouter } from '../Router2';
// export default (state, action) => {
//     const newState = RootRouter.router.getStateForAction(action, state);
//     return newState || state;
// };